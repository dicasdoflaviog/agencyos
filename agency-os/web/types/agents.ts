export type AgentType = 'oracle' | 'vera' | 'atlas' | 'vox'

export const AGENT_LABELS: Record<AgentType, string> = {
  oracle: 'ORACLE',
  vera: 'VERA — Copywriter',
  atlas: 'ATLAS — Design',
  vox: 'VOX — Áudio',
}
